type ItemsSlotProps = {
  params: Promise<{ id: string }>
}

const ItemsSlot = async ({ params }: ItemsSlotProps) => {
  return (
    <></>
    //<BoxTable searchParams={{}} />
  )
}

export default ItemsSlot
